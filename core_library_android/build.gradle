buildscript {

    ext.kotlin_version = "1.3.40"
    ext.junit_version = "4.12"
    ext.library_core_version = "0.1.5-33"
    ext.library_android_version = "0.1.5-33"

    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.1'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "org.jetbrains.kotlinx:kotlinx-metadata-jvm:0.0.6"
    }
}

wrapper {
    gradleVersion = "5.3.1"
    distributionType = "ALL"
}

allprojects {
    repositories {
        maven {
            url "s3://maven.apptronic.net/"
            credentials(AwsCredentials) {
                accessKey "AKIAQYS4TMFUDDMKIPNL"
                secretKey "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"
            }
        }
        google()
        mavenCentral()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}


task publishLibToMavenApptronicNet(dependsOn: [
        ':library:bundleDebugAar',
        ':library:publish'
])

task publishLibToMavenLocal(dependsOn: [
        ':library:bundleDebugAar',
        ':library:publishToMavenLocal'
])